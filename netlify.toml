[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# STRAPI_URL must be set in the Netlify UI (Site settings → Environment variables)
# to the Strapi Cloud URL, e.g. https://your-project.strapiapp.com
